// Future Lambda handler for API Gateway / Lambda deployment (Step 15a).
// This will reuse the bootstrap factory from src/bootstrap/ to create
// the NestJS app and delegate API Gateway events to it.
// Not implemented — see docs/architecture/application-boundaries.md.
